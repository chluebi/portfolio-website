syntax = "proto3";

package portfolio;

message Projects {
    repeated Project projects = 1;
}

message Project {
    int32 id = 1;
    string title = 2;
    optional string url = 3;
    repeated string languages = 4;
    repeated string tags = 5;
}